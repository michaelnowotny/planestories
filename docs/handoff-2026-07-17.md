# planestories — session handoff (2026-07-17)

You are picking up planestories. This tells you the state, how to work in the repo, the gotchas
already paid for, and where everything lives. Read `docs/plan-production-feedback-2026-07.md`
next — that is WHAT to build. This doc is HOW and WHERE.

## Progress update (2026-07-17, later)

**v2 slices 1–3 are DONE, on `main`, pushed** (`25a0132`, `417c64f`, `ec62e39`). 164 tests green;
tsc + biome clean. These were what the finance session was waiting on for its ~80-ticket close.

- **Slice 1 — P0-2 rate-limit backoff** (`client.ts`): every `request()` retries 429/5xx/network
  with `Retry-After` (else exponential backoff + jitter, capped 30s). Knob `PLANE_MAX_RETRIES`
  (default 5) → `ResolvedConfig.maxRetries` → all 5 client sites. `--max-retries` CLI flag deferred.
- **Slice 2 — P0-1 skip-unchanged** (`content-hash.ts` + `importer.ts`): `payloadHash()` over the
  RENDERED payload, stored as `plane_hash`. A linked story whose hash matches → action `unchanged`,
  zero writes. `--force` bypasses. Hash-in-file, NOT a live GET (decision #2). Out-of-band-edit gap
  is owned by future groom.
- **Slice 3 — P1-2 `--status-only`** (`importer.ts` + `issues.ts`): PATCH only `state` of linked
  items; unlinked skipped with a warning; no `plane_hash` write-back (honest partial sync);
  `UpdateWorkItemInput.name` made optional so status-only never clobbers a board title.

**NEXT = slice 4:** shared `fetchProjectIndex(projectId)` (one paginated `list_work_items` with
`byId`/`byIdentifier`/`byNormalizedTitle`/`childrenByParent` maps — extend `fetchWorkItems` in
`issues.ts`, do NOT add a parallel path) + **P0-3 duplicate guard** on create (skip-with-warning by
default; `--adopt-duplicates` behind exact-normalized-title match, multi-match = hard error). Then
slice 5 (export completeness), 6 (groom), 7 (P2 batch). Live real-board validation of 1–3 in the
Bloomenkohlberg SANDBOX is the finance session's step (they report back per slice).

**Repo note:** local git identity was set (`git config user.name/email`) because the box's hostname
stopped auto-resolving mid-session; commits use `Michael Nowotny <michaelnowotny@pop-os.lan>` to
match history.

## Where things stand

- **v0.1.0 is done, merged to `main`, tagged `v0.1.0`, pushed.** 144 tests green; tsc + biome clean.
  The Linear->Plane port plus two rounds of production feedback are all in. `feat/plane-port`
  branch is kept intentionally.
- **Release:** tagging `v*` builds cross-platform binaries via `.github/workflows/release.yml`
  (single GitHub-hosted ubuntu job, Bun cross-compiles all 5 targets). No self-hosted runners.
- **npm publish is pending** (Michael's task). The name `planestories` is free. Note it's a **Bun**
  package: `bin` is `src/cli/index.ts` with a `#!/usr/bin/env bun` shebang, so `bunx planestories`
  is the clean install; `npm i -g` needs Bun on PATH.
- **Next work = the v2 improvement program** in `docs/plan-production-feedback-2026-07.md`.
  Decisions are LOCKED (see that doc's "Decisions (LOCKED...)" section). Sequenced 1->7. The
  finance production session is the live first consumer and is waiting on slices 1-3 (backoff,
  skip-unchanged, `--status-only`) for an ~80-ticket bulk close within a couple of days.

## How to work in this repo (paid-for gotchas)

- **Bun isn't on the default PATH:** `export PATH="$HOME/.bun/bin:$PATH"` in every shell. Then
  `bun install`. Keep green before any commit: `bun test`, `bunx tsc --noEmit`,
  `bunx biome check ./src ./tests` (and `bunx biome check --write` to auto-fix format + import sort).
- **Tests use a fake client** at `tests/helpers/fake-plane-client.ts`. When you add a `PlaneClient`
  method, add it to the fake too, or real-import tests throw (this bit us with `projectBoardUrl`).
- **Edit-tool tab gotcha:** biome indents with TABS. Matching leading whitespace in the Edit tool
  fails silently. Match an inner substring (no leading whitespace) and let `biome --write` reindent.
- **Live testing:** use the **Bloomenkohlberg SANDBOX** project (identifier BLOOM), workspace slug
  `bloomenkohlberg`, creds already in `.env`. NEVER test against the real **Data Platform** board
  (identifier DATA, ~815 production items). Clean up sandbox items with `delete ... --yes`. The
  `planestories` and `archived` labels already exist in BLOOM from prior testing.
- **Secrets:** `.env` holds real credentials, gitignored. Never print or commit them.

## Plane REST facts already discovered (don't rediscover)

- CRUD path segment is `/issues/` (not `/work-items/`). Archive is `/work-items/{id}/archive/` but
  Plane only archives completed/cancelled items — so we archive via a **label** instead (there is no
  `archiveWorkItem` on the client; `delete --archive` applies the `archived` label).
- The `external_id` filter returns **HTTP 404** (not an empty list) on a miss. `client.request` has
  an `allowNotFound` option; `findWorkItemByExternalId` uses it.
- The list endpoint returns `description_html` by default; pass `expand=state,assignees,labels` for
  related names. `fetchWorkItems` already does this.
- 429/rate-limit handling belongs in `client.request` (that's slice 1). `fetchProjectIndex`
  (slice 4 shared infra) should extend `fetchWorkItems` in `src/plane/issues.ts`, not add a parallel
  path.

## Architecture map (where to make each change)

- `src/plane/client.ts` — REST client: `request()` (429 backoff lands here), `listAll()` cursor
  pagination, per-endpoint methods, `deriveWebBaseUrl`, `workItemWebUrl`/`projectBoardUrl`.
- `src/plane/issues.ts` — `createWorkItem`/`updateWorkItem`/`findWorkItemByExternalId`/
  `fetchWorkItems` + `FetchedWorkItem` (carries parent/external_id/stateGroup). Extend for
  `fetchProjectIndex`, export `kind`/`parent`.
- `src/plane/resolvers.ts` — project/state/label/member resolution; `findProject` (uuid/name/
  identifier + Levenshtein suggest), `firstStateIdInGroups` (reuse for groom state closure).
- `src/sync/importer.ts` — import flow: resolve, (hash-skip goes here), create/update, `--source-label`,
  `syncCriteriaChildren`. `exporter.ts`, `deleter.ts`, `setter.ts` are the other verbs.
- `src/markdown/` — `parser.ts` (reads yaml keys; new keys parse here), `serializer.ts` (writes them),
  `writer.ts` (`writeBackIds` + `clearWriteBack`), `criteria.ts` (`splitBody`/`buildAcceptanceCriteria`),
  `html.ts` (`markdownToHtml` via marked, `htmlToMarkdown` via turndown with a task-list rule).
- `src/cli/commands/` — `import/export/delete/set/projects`. New commands (`groom`, `doctor`) go here
  and register in `src/cli/index.ts`.
- `src/types.ts` — `UserStory`, `ResolvedConfig`, `ImportResult`/`Summary`, `PLANE_PRIORITIES`. New
  yaml keys thread through types + parser + serializer.
- `tests/` mirrors `src/`. `tests/helpers/fake-plane-client.ts` is the fake.

## Idempotency + identity contract (load-bearing)

- Created items are stamped `external_source: "planestories"` and `external_id = slug(title)`.
  Criterion sub-items: `external_id = "${parentExternalId}::ac${i}"`, `parent` set, state from the
  checkbox. `kind=criterion` is detected by that `::ac\d+` external_id — the groom cascade must gate
  on THIS, never on "parent is Done" (see plan doc's CRITICAL constraint).
- Project can be given by display name OR identifier (e.g. `DATA`); `--project` overrides
  frontmatter which overrides per-story `project:` — no wait: precedence is `--project` >
  per-story `project:` > frontmatter > `defaultProject`. (Confirm in importer if unsure.)

## Records / history

- The full v0.1.0 build arc is in `git log` (14 detailed commit messages).
- Feedback that shaped it: in the finance repo `external_info/planestories-feedback.md` and
  `planestories-feedback-v2-fromtest-2026-06-24.md`.
- Canonical usage reference: `docs/USING_WITH_CLAUDE.md`. Session orientation (finance side):
  `external_info/planestories-orientation.md`, referenced from finance `CLAUDE.md`.

## Small open items (not blocking)

- Finance repo `.mcp.json` (the Plane MCP config) is untracked; commit-vs-user-scope decision is
  pending. It's OAuth-only, no secrets. Recommendation: user scope (`-s user`), available across all
  projects.
- Finance `external_info/planestories-phase3-test-prompt.md` and `planestories-v2-retest.md` are
  historical/superseded; `planestories-orientation.md` is the canonical one.

## First prompt for the next session

```
You are picking up planestories development. Read, in order:
1. ~/PycharmProjects/planestories/docs/handoff-2026-07-17.md   (state, how to work here, gotchas)
2. ~/PycharmProjects/planestories/docs/plan-production-feedback-2026-07.md   (what to build; decisions LOCKED)

State: v0.1.0 released; v2 slices 1–3 (backoff, skip-unchanged, --status-only) are DONE on main
(25a0132, 417c64f, ec62e39), 164 tests green. Next is slice 4 in the plan doc (sequenced 1->7).
Decisions are locked: duplicates skip-with-warning (adopt only behind --adopt-duplicates, exact-title
match only, multiple matches = hard error); skip-unchanged via a content hash of the RENDERED payload
stored as `plane_hash`; groom closes ONLY kind=criterion children (never story children of a Done epic);
yaml keys plane_hash/parent/kind/comment; do NOT add plane_status.

Start with slice 4: build the shared fetchProjectIndex(projectId) — ONE paginated list with
byId/byIdentifier/byNormalizedTitle/childrenByParent maps, by EXTENDING fetchWorkItems in
src/plane/issues.ts (not a parallel path) — then the P0-3 duplicate guard on create (skip-with-warning
default; --adopt-duplicates behind exact-normalized-title match; multi-match = hard error). Then
slice 5 (export completeness), 6 (groom), 7 (P2 batch).

Setup: cd ~/PycharmProjects/planestories && export PATH="$HOME/.bun/bin:$PATH" && bun install.
Keep green each step: bun test; bunx tsc --noEmit; bunx biome check ./src ./tests.
Live-test only in the Bloomenkohlberg SANDBOX (BLOOM, creds in .env) — never the DATA board.
Commit per slice with a descriptive message; the finance session reports back real-board numbers after each.
```
