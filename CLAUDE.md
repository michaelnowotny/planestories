# See `AGENTS.md`

The agent guide for this repository lives in **`AGENTS.md`** (architecture map, working rules,
conventions) — it is agent-agnostic and kept in one place so it cannot drift.

Start with **`docs/HANDOFF.md`** for current state, roadmap, and gotchas.
