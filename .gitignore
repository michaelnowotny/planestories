# dependencies
node_modules

# output
out
dist
*.tgz

# compiled binaries
planestories
planestories-*
*.exe

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# config files with secrets
.planestoriesrc.json
.linearrc.json

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Vercel
.vercel

# Default CLI output paths. These are working artifacts of a real run — they contain
# whole-board content (and can carry internal infrastructure detail), and a stray
# `git add -A` after a smoke test is exactly how they end up in history forever.
/atlas.json
/atlas.html
/exported-stories.md
/*.snapshot.json
/*.snapshot.json.apply-*.journal.jsonl
