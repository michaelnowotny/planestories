# dependencies
node_modules

# output
out
dist
*.tgz

# compiled binaries
planestories
planestories-*
*.exe

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# config files with secrets
.planestoriesrc.json
.linearrc.json

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Vercel
.vercel

# EXPORTS. Everything a command writes out of a board — atlas renders, story exports,
# spec packets, snapshots, reports — goes here, and none of it is ever committed. Board
# exports are DATA: large, private to somebody's project, and sometimes carrying
# internal infrastructure detail. A `git add -A` after a smoke run once put 49,258 lines
# of live board content into a branch; this directory exists so that cannot recur.
/exports/

# The old default output paths, still ignored so a stale checkout or an older build
# cannot reintroduce the same accident.
/atlas.json
/atlas.html
/exported-stories.md
/*.snapshot.json
/*.snapshot.json.apply-*.journal.jsonl
